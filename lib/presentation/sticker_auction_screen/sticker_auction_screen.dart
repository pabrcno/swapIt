import 'package:flutter/material.dart';

class StickerBidScreen extends StatelessWidget {
  const StickerBidScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
